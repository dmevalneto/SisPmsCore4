@model StatusManutencao
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutManutencao.cshtml";
}

<h2 class="text-center text-info">Status</h2>
<hr />
<p>
    @*<a class="btn btn-default" asp-action="Create">Novo Registro</a>*@
    <button class="btn btn-primary" onclick="NovoRegistro()">Novo Registro</button>
</p>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.IdStatusManutencao)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Observacao)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in (List<StatusManutencao>)ViewBag.ListaStatusManutencao)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdStatusManutencao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Observacao)
                    </td>
                    <td>
                        @Html.ActionLink("Editar", "Edit", new { id = item.IdStatusManutencao }, new { @class = "btn btn-success" }) |
                        @Html.ActionLink("Detalhes", "Details", new { id = item.IdStatusManutencao }, new { @class = "btn btn-secondary" }) |
                        @Html.ActionLink("Deletar", "Delete", new { id = item.IdStatusManutencao }, new { @class = "btn btn-danger" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


<script>
    function NovoRegistro() {
        window.location.href = "../StatusManutencao/Create";
    }
</script>


