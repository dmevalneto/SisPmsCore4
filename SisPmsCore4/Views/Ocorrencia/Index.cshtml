@model Ocorrencia
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutSp.cshtml";
}

<h2 class="text-center text-info">Ocorrência</h2>
<hr />
<p>
    @*<a class="btn btn-default" asp-action="Create">Novo Registro</a>*@
    <button class="btn btn-primary" onclick="NovoRegistro()">Novo Registro</button>
</p>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.idOcorrencia)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Numero)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Descricao)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in (List<Ocorrencia>)ViewBag.ListaOcorrencia)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.idOcorrencia)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Numero)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descricao)
                    </td>
                    <td>
                        @Html.ActionLink("Editar", "Edit", new { id = item.idOcorrencia }, new { @class = "btn btn-success" }) |
                        @Html.ActionLink("Detalhes", "Details", new { id = item.idOcorrencia }, new { @class = "btn btn-secondary" }) |
                        @Html.ActionLink("Deletar", "Delete", new { id = item.idOcorrencia }, new { @class = "btn btn-danger" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


<script>
    function NovoRegistro() {
        window.location.href = "../Ocorrencia/Create";
    }
</script>


